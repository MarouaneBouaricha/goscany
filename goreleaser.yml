builds:
- binary: goscany
  main: ./cmd/
  goos:
  - linux
  goarch:
  - amd64
  - arm64
  - darwin
  env:
  - CGO_ENABLED=0
  flags:
  - -mod=vendor

release:
  prerelease: auto

universal_binaries:
- replace: true

checksum:
  name_template: 'checksums.txt'